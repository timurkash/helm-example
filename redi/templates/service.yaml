apiVersion: v1
kind: Service
metadata:
  name: {{ include "redi.fullname" . }}
  labels:
    {{- include "redi.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "redi.selectorLabels" . | nindent 4 }}
