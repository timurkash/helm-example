---
# Source: sfpg/templates/serviceaccount.yaml
apiVersion: v1
kind: ServiceAccount
metadata:
  name: RELEASE-NAME-sfpg
  labels:
    helm.sh/chart: sfpg-0.1.0
    app.kubernetes.io/name: sfpg
    app.kubernetes.io/instance: RELEASE-NAME
    app.kubernetes.io/version: "1.16.0"
    app.kubernetes.io/managed-by: Helm
---
# Source: sfpg/templates/service.yaml
apiVersion: v1
kind: Service
metadata:
  name: RELEASE-NAME-sfpg
  labels:
    helm.sh/chart: sfpg-0.1.0
    app.kubernetes.io/name: sfpg
    app.kubernetes.io/instance: RELEASE-NAME
    app.kubernetes.io/version: "1.16.0"
    app.kubernetes.io/managed-by: Helm
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: sfpg
    app.kubernetes.io/instance: RELEASE-NAME
---
# Source: sfpg/templates/deployment.yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: RELEASE-NAME-sfpg
  labels:
    helm.sh/chart: sfpg-0.1.0
    app.kubernetes.io/name: sfpg
    app.kubernetes.io/instance: RELEASE-NAME
    app.kubernetes.io/version: "1.16.0"
    app.kubernetes.io/managed-by: Helm
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: sfpg
      app.kubernetes.io/instance: RELEASE-NAME
  template:
    metadata:
      labels:
        app.kubernetes.io/name: sfpg
        app.kubernetes.io/instance: RELEASE-NAME
    spec:
      serviceAccountName: RELEASE-NAME-sfpg
      securityContext:
        {}
      containers:
        - name: sfpg
          securityContext:
            {}
          image: "nginx:0.1.0"
          imagePullPolicy: Always
          ports:
            - name: http
              containerPort: 80
              protocol: TCP
          livenessProbe:
            httpGet:
              path: /
              port: http
          readinessProbe:
            httpGet:
              path: /
              port: http
          resources:
            {}
---
# Source: sfpg/templates/tests/test-connection.yaml
apiVersion: v1
kind: Pod
metadata:
  name: "RELEASE-NAME-sfpg-test-connection"
  labels:
    helm.sh/chart: sfpg-0.1.0
    app.kubernetes.io/name: sfpg
    app.kubernetes.io/instance: RELEASE-NAME
    app.kubernetes.io/version: "1.16.0"
    app.kubernetes.io/managed-by: Helm
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['RELEASE-NAME-sfpg:80']
  restartPolicy: Never
