apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "sfpg.fullname" . }}-test-connection"
  labels:
    {{- include "sfpg.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "sfpg.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
